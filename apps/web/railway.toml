[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && pnpm --filter @miraculturee/shared build && cd apps/web && pnpm build"

[deploy]
startCommand = "npx serve dist -s -l tcp://0.0.0.0:${PORT:-3000}"
